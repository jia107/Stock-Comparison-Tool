# Stock-Comparison-Tool
## Installation &amp; Requirements  To run this project locally, clone the repository, navigate to the project directory, and install the dependencies using the included `requirements.txt` file:  ```bash pip install -r requirements.txt
